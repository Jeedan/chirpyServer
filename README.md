# Chirpy Server

Chirpy is a HTTP server that uses password and email authentication for User creation and storing said users in a Postgres database using drizzle. Once a user has been authenticated they can then post `chirps` which are short 140 character long messages.

Documentation will be coming...just been a bit lazy